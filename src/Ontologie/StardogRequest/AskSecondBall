ASK WHERE {
    ?cible a grid:Ball2 .
    ?cible2 a grid:Ball3 .
    ?NotWall a grid:NotWall .
    ?Ball rdf:type grid:Ball1 .
    
    ?Ball grid:hasNorth ?c .
    
    FILTER (?c = ?NotWall && (?c = ?cible || ?c = ?cible2))
}